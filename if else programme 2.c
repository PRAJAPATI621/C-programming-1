// Online C complire to run C program online
#include<stdio.h>
int main()
{
 int a,b;
 printf("Enter two number\n");
 scanf("%d %d",&a,&b);
  if(a>b)
 {
 printf("%d is the larger number and %d is the smaller number among the number",a,b);
 }
 else
 {
  if(a<b)
  {
   printf("%d is the smaller number and %d is the larger number among the number",a,b);
   }
 else
 {
  printf("both number are equal");
 }
 }
 return 0;
}







